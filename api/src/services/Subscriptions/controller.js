const models = require("models");
const asyncLib = require("async");
const { Op } = require("sequelize");

module.exports = {
	getSubscriptions: function (res) {},
	getSubscription: function (res, id) {},
	getUserSubscription: function (res, id) {},
	createSubscription(res, data) {},
	updateSubscription: function (res, id, data) {},
	deleteSubscription: function (res, id) {},
};
